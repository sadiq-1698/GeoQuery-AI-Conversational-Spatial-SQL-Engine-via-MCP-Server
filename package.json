{
  "name": "geoquery-ai",
  "private": true,
  "description": "Conversational Spatial SQL Engine via MCP Server",
  "workspaces": [
    "apps/web",
    "services/mcp-server"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
